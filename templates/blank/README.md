# blank bundle

A plain Ubuntu 24.04 VPS — no app installed. The default.

A "bundle" is just a folder here with an optional `install.sh` (runs inside the guest on
first boot) and an optional `port` file (the app's HTTP port, used for the Cloudflare route).
`blank` has neither.
